public int main() {
    int i=0; 
    int[] a; 

    starttime();
    a=new int[8];
    while (!({a[i]=i; i=i+1;} i<8))
      if (i>=8) break;
    putarray(i, new int[10]);
    while (true==1) {
      i=getint();
      if (i) break;
      if (!i) break;
    }
    stoptime();
    return a;
}
