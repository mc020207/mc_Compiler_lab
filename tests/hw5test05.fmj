public int main() {
    int i=0; 
    int[] a; 

    starttime();
    a=new int[8];
    while (!({a[i]=i; i=i+1;} 0))
      if (i>=8) break;
    putarray(8,a);
    while (true==1) {
      a=getint();
      if (i) break;
      if (!i) break;
    }
    stoptime();
    return a[1000];
}
